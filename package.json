{
  "devDependencies": {
    "nodemon": "^2.0.20",
    "serverless-localstack": "^1.0.1",
    "serverless-mocha-plugin": "^1.12.0",
    "serverless-offline": "^11.5.0"
  },
  "scripts": {
    "test": "sls invoke test --path test",
    "start": "yarn nodemon --exec yarn offline",
    "offline": "sls offline start --host 0.0.0.0"
  },
  "name": "tooling-s3-sqs",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1261.0"
  }
}
